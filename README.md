# React Template

Basic React project using default Vite settings.

TO DO:

- Set repo name in vite.config.js
- (Potentially) add pages url as homepage to package.json
- `npm run deploy` will build app and push it to `gh-pages` branch
